{% extends "templates/mail.twig" %}
{% block body %}
    <h1>Hola {{ persona.nombre }}!</h1>
    <p>Este mail es un recordatorio para el pago de {{ pago.concepto }}</p>
    <p>Ya puede abonar la candidad de <b>{{ pago.cantidad }} €</b>.</p>

    {{ pago.getPagoRecurrente().forma_pago | raw }}

    <p>Una vez realice el abono pulse el enlace de aquí abajo para notificarlo.</p>
    {% include "templates/mail/button.twig" with {href: this.getLink('GLApp\\publico\\confirmar', {token: pago.token}, true), text: "Confirmar abono" } %}
    <p>Tambien puede responder a este email para confirmarlo.</p>
    <p>Un saludo!</p>
    <small>Cod. Pago #{{ pago.token }}</small>
{% endblock %}